name: Publish Skills

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Validate SKILL.md files
        run: |
          echo "Checking for SKILL.md files..."
          find skills -name "SKILL.md" | wc -l | xargs -I {} echo "Found {} skill files"
          
          echo "Validating structure..."
          for skill in skills/*/; do
            if [ -f "${skill}SKILL.md" ]; then
              echo "✓ $(basename $skill) has SKILL.md"
            else
              echo "✗ $(basename $skill) missing SKILL.md"
              exit 1
            fi
          done

  publish:
    runs-on: ubuntu-latest
    needs: validate
    if: github.ref == 'refs/heads/main'
    
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Create Release
        run: |
          echo "Publishing skills to registry..."
          # Placeholder for actual publishing logic
          # This would depend on the specific skills registry
